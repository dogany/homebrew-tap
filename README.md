# Dogany Homebrew Tap

Homebrew tap for [Dogany](https://github.com/dogany) apps.

## Cyclope

Window snapping and layout manager for macOS.

```sh
brew tap dogany/tap
brew install --cask cyclope
```

Or as a one-liner without tapping first:

```sh
brew install --cask dogany/tap/cyclope
```
